<%= render 'user', user: @user %>
<div><p>Bio:

<%=@user.bio%>
</p></div>
<div>
<p>Latest posts from <%=@user.name%>:</p>
<%= render partial: 'recent_post', collection: @recent_posts %>
<a href="/users/<%=@user.id%>/posts/">See all posts from <%=@user.name%></a>
</div>