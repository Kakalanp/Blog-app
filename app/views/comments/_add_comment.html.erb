     <%= form_with model: @comment, url: user_post_comments_path(@user.id, @post.id) do |form| %>
        <%= form.text_field :text %>
        <%= form.submit "Add new comment as "%>
    <% end %>
    